{
  "permissions": {
    "deny": [
      "Edit(//.env)",
      "Edit(//.env.*)",
      "Write(//.env)",
      "Write(//.env.*)",
      "Read(~/.ssh/**)",
      "Edit(~/.ssh/**)",
      "Write(~/.ssh/**)"
    ]
  }
}
