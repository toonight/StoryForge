{
  "permissions": {
    "deny": [
      "Read(//.env)",
      "Read(//.env.*)",
      "Edit(//.env)",
      "Edit(//.env.*)",
      "Write(//.env)",
      "Write(//.env.*)",
      "Read(~/.ssh/**)",
      "Edit(~/.ssh/**)",
      "Write(~/.ssh/**)"
    ]
  }
}
